import {ContainerFooter} from './styles'

const FooterProp = ({title, description,description1,description2}) => (

    <ContainerFooter>
        <h3>{title}</h3>
        <p>{description}</p>
        <p>{description}</p>
        <p>{description}</p>


    </ContainerFooter>

);

export default CardVeg;